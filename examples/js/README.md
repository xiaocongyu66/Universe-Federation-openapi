# JavaScript 示例

需要 Node 18+（内置 `fetch`）。

```bash
INSTANCE=https://example.com node meta.js
INSTANCE=https://example.com TOKEN=xxx node create-note.js
INSTANCE=https://example.com CLIENT_ID=xxx REDIRECT_URI=https://app/callback node oauth-pkce.js
```
