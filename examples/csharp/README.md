# C# 示例（.NET 6+）

可将文件放入 `dotnet new console` 项目，或使用 `dotnet-script`：

```bash
dotnet script Meta.cs
INSTANCE=https://example.com TOKEN=xxx dotnet script CreateNote.cs
dotnet script OauthPkce.cs
```

也可直接复制方法到 ASP.NET / 桌面应用中。
