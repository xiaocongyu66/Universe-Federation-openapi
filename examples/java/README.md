# Java 示例（Java 11+）

```bash
javac Meta.java && INSTANCE=https://example.com java Meta
javac CreateNote.java && INSTANCE=https://example.com TOKEN=xxx java CreateNote
javac OauthPkce.java && java OauthPkce
```
